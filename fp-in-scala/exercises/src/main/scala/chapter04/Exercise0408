Exercise 4.8

In order to report both errors I would adjust the error type to be a List of strings.

The problem with reporting both errors is that the `mkAge(age)` that is being passed to `map2`
will not be executed if the `mkName(name)` already yields an error.
